services:
  - type: web
    name: gjfortunesinais
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: ADMIN_PASSWORD
        sync: false
      - key: SITE_SECRET_KEY
        generateValue: true
